import { useApiResultContext } from "@/contexts/apiResult"

export function RideCard () {
    const {  } = useApiResultContext()
    return (
        <div>
            <h3></h3>
        </div>
    )
}